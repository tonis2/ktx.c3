{
  // Language version of C3.
  "langrev": "1",
  // Warnings used for all targets.
  "warnings": [ "no-unused" ],
  // Directories where C3 library files may be found.
  "dependency-search-paths": [ "lib" ],
  "dependencies": ["getopt", "image"],
  "authors": [ "Tonis" ],
  "version": "0.1.0",
  "sources": [ "src/**" ],
  "test-sources": [ "test/**" ],
  "build-dir": "build",
  // Output location, relative to project file.
  "output": "build",
  // basis_universal (ETC1S/UASTC + BasisLZ, via src/ktx/basis.c3) ships a
  // prebuilt static lib per platform under <arch>/libbasisu.a. It bundles a full
  // zstd, which also satisfies the container's ZSTD_* calls, so no separate
  // system libzstd is needed. "c++" links the C++ runtime basis_universal needs.
  "linked-libraries": [ "basisu", "c++" ],
  // The new macOS linker (ld-prime) hard-errors on foreign files found in
  // search paths (linux-x64/libbasisu.a, the linux libc++.so shim) instead of
  // skipping them, so each platform gets its own target with only its own
  // lib directory. `c3c build` builds the default (macOS) target; on linux use
  // `c3c build ktx-linux`.
  "linker-search-paths": [ "macos-aarch64" ],
  "targets": {
    "ktx": {
      // Executable or library.
      "type": "executable"
    },
    "ktx-linux": {
      "type": "executable",
      "linker-search-paths": [ "linux-x64", "linux-aarch64" ]
    }
  },
  // Global settings.
  // CPU name, used for optimizations in the LLVM backend.
  "cpu": "generic",
  // Optimization: "O0", "O1", "O2", "O3", "O4", "O5", "Os", "Oz".
  "opt": "O0"
  // See resources/examples/project_all_settings.json and 'c3c --list-project-properties' to see more properties.
}